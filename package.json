{
  "name": "audittoolbox-mcp",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web-ui",
    "build": "npm run build --workspace=apps/web-ui",
    "lint": "npm run lint --workspace=apps/web-ui",
    "type-check": "tsc --noEmit --project apps/web-ui/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
